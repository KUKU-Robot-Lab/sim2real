"""S2R 배포 콘솔 — 기존 미션 러너·게이트·status 를 한 화면에 모은다.

두 프로세스다. **API 프로세스(이 패키지의 대부분)는 rclpy 를 import 하지 않는다** —
그래서 DDS 참가자가 될 수 없고, 어떤 버그가 나도 로봇 토픽에 직접 발행할 수 없다.
ROS 를 보는 것은 `bridge_node` 하나이고 그것은 구독만 한다(발행자·서비스 클라이언트 0).
로봇에 닿는 모든 동작은 미션 yaml 에 적힌 **기존 도구의 argv** 를 자식 프로세스로 띄우는 것뿐이다.
"""
